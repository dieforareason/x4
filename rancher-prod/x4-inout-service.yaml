apiVersion: v1
kind: ConfigMap
metadata:
  labels:
    app: x4-inout
  namespace: x4-s24
  name: x4-inout-configmap
data:
  application.properties: |
    spring.profiles.active=development
    spring.datasource.url=jdbc:mysql://mysql-service:3306/x4_inout?useLegacyDatetimeCode=false&serverTimezone=UTC
    spring.datasource.username=${mysql-user}
    spring.datasource.password=${mysql-password}
    spring.activemq.broker-url=tcp://activemq-service:61616
    spring.activemq.username=${activemq-user}
    spring.activemq.password=${activemq-password}
    spring.redis.url=redis-service
    spring.redis.port=6379
    spring.config.use-legacy-processing=true
    server.port=8080
    server.servlet.contextpath=/
    inout.handlers.id='DOCUMENT_EXPORT,QUOTE_IMPORT,ACCURATE_IMPORT,TAXINVOICE_EXPORT'
    inout.handlers.name='Document Export,QUOTE IMPORT,Import Accurate Data,Generate Tax Invoice'
    feign.blobstore-service=http://x4-blobstore-service:8080/
    feign.document-service=http://x4-document-service:8080/
    feign.inventory-service=http://x4-inventory-service:8080/
    feign.task-service=http://x4-task-service:8080/task
    feign.finprocess-service=http://x4-finprocess-service:8080/
    feign.uoa-service=http://x4-uoa-service:8080/
    jwt.signing-key=${jwt-signing-key}
    email.jira=test-notifications@asyx.com
    management.endpoints.web.exposure.include=health,info
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: x4-inout
  name: x4-inout-service
  namespace: x4-s24
spec:
  type: NodePort
  selector:
    app: x4-inout
  ports:
  - protocol: TCP
    port: 8080
    targetPort: 8080
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: x4-inout
  namespace: x4-s24
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 25%
  progressDeadlineSeconds: 600
  selector:
    matchLabels:
      app: x4-inout
  template:
    metadata:
      labels:
        app: x4-inout
        log-format: json
    spec:
      priorityClassName: low-priority
      containers:
      - name: x4-inout
        image: pkg.asyx.com:5000/asyx/x4-inout-application:4.3.13-144 
        imagePullPolicy: Always
        resources:
          requests:
            memory: 512Mi
            cpu: 100m
        ports:
        - containerPort: 8080
          protocol: TCP
        env:
        - name: mysql-user
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: mysql-asyx-user
        - name: mysql-password
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key:  mysql-asyx-password
        - name: jwt-signing-key
          valueFrom:
            secretKeyRef:
              name: jwt-signing-key-secret
              key: secret
        - name: activemq-user
          valueFrom:
            secretKeyRef:
              name: activemq-secret
              key: activemq-user
        - name: activemq-password
          valueFrom:
            secretKeyRef:
              name: activemq-secret
              key: activemq-password
        volumeMounts:
        - name: x4-inout-config
          mountPath: /x4/config/application.properties
          subPath: application.properties
        livenessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          failureThreshold: 3
          periodSeconds: 10
          initialDelaySeconds: 180
        readinessProbe:
          httpGet:
            path: /actuator/health
            port: 8080
          failureThreshold: 3
          periodSeconds: 10
          initialDelaySeconds: 60
      imagePullSecrets:
      - name: asyx-docker-prod-secret
      volumes:
      - name: x4-inout-config
        configMap:
          name: x4-inout-configmap