apiVersion: v1
kind: Service
metadata:
  labels:
    app: mysql
    service: mysql
  name: proxy-mysql-service
  namespace: x4-s24
spec:
  type: NodePort
  selector:
    app: mysql
  ports:
  - name: tcp-transfer
    port: 3306
    targetPort: 33066